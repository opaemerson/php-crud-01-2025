{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Listen for Xdebug",
            "type": "php",
            "request": "launch",
            "port": 9000,
            "hostname": "localhost",
            "stopOnEntry": false,
            "pathMappings": {
                "/var/www/html/php-crud-01-2025": "${workspaceFolder}"
            }
        }
    ]
}